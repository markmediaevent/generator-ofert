{
  "name": "generator-ofert-markmedia",
  "version": "5.0.0",
  "private": true,
  "description": "Generator ofert Mark Media - wersja bez zewnętrznych zależności",
  "main": "server.js",
  "engines": { "node": ">=20 <23" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check vendor/mini-express.js"
  }
}
